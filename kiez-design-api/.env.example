# --- Pflicht ---
OPENAI_API_KEY=sk-...
PORT=3100

# --- Shopify App Proxy (Pflicht für Live, für den lokalen Test leer lassen) ---
SHOPIFY_APP_SECRET=
# Wenn leer: Signaturprüfung AUS (nur Test!)

# --- Missbrauchsschutz ---
LIMIT_PER_SESSION=3
LIMIT_PER_IP_DAY=10
LIMIT_GLOBAL_DAY=300
MAX_PARALLEL_JOBS=3
TURNSTILE_SECRET=
# Wenn leer: Turnstile AUS (nur Test!)

# --- Bild ---
IMAGE_MODEL=gpt-image-1
IMAGE_SIZE=1024x1536
IMAGE_QUALITY=medium
QA_MODEL=gpt-4o-mini
PUBLIC_BASE_URL=https://mcp.markusassemacher.de/kiez
PREVIEW_TTL_DAYS=7
